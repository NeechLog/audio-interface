```python
from audiomessages import AudioMessage, ProcessingMetadata, AudioMessageInfo

# Create an audio message
audio_msg = AudioMessage()
audio_msg.text = "Hello World"
audio_msg.audio_binary = b"audio_data"

# Create metadata
processing_meta = ProcessingMetadata()
processing_meta.request_id = "request-123"
processing_meta.status_code = 200
```
