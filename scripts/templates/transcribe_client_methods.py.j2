def transcribe(self, request: {{ pb2_module_name }}.TranscribeRequest) -> {{ pb2_module_name }}.TranscribeResponse:
        """Unary transcribe call."""
        return self.stub.Transcribe(request)
    
def stream_transcription(self, requests):
    """Bidirectional streaming for transcription."""
    return self.stub.StreamTranscription(requests)
